<p id="notice"><%= notice %></p>

<h1>Статьи</h1>

<section id='posts'>
 <%= render @posts %>
</section>

<div class="actions">
  <% if user_signed_in? %>
    <%= link_to 'Новая статья', new_post_path, class: 'btn btn-primary'  %>
  <% end %>
</div>
