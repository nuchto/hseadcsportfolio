<%= form_with(model: projectportfolio, local: true) do |form| %>
  <% if projectportfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(projectportfolio.errors.count, "error") %> prohibited this projectportfolio from being saved:</h2>

      <ul>
      <% projectportfolio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class=”field”>
   <%= form.label :workportfolio %>
   <%= form.file_field :workportfolio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
