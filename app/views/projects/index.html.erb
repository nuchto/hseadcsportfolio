
  <h1 class="my-auto">Проекты</h1>
  <div class="actions">
    <% if user_signed_in? %>
      <%= link_to 'Создать проект', new_project_path, class: 'btn btn-dark btn-lg'  %>
    <% end %>
  </div>

<section id='projects'>
  <%= render @projects %>
</section>
